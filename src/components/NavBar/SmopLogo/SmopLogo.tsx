import { ReactNode } from 'react';
import styles from './styles.module.css';


const SmopLogo = (): ReactNode => {
  return (
    <div className={styles.logo}>
      smop
    </div>
  );
}

export default SmopLogo;